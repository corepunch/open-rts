# server

Future headless simulation and network authority code belongs here.

This directory is intentionally empty for now; defining the boundary keeps
client-only rendering decisions from leaking into future server code.
